### `RightToolbar`

`index.vue`

```vue
<template>
  <div
    class="top-right-btn"
    :style="style">
    <el-row>
      <el-tooltip
        class="item"
        effect="dark"
        :content="showSearch ? '隐藏搜索' : '显示搜索'"
        placement="top"
        v-if="search">
        <el-button
          circle
          icon="Search"
          @click="toggleSearch()" />
      </el-tooltip>
      <el-tooltip
        class="item"
        effect="dark"
        content="刷新"
        placement="top">
        <el-button
          circle
          icon="Refresh"
          @click="refresh()" />
      </el-tooltip>
      <el-tooltip
        class="item"
        effect="dark"
        content="显隐列"
        placement="top"
        v-if="Object.keys(columns).length > 0">
        <el-button
          circle
          icon="Menu"
          @click="showColumn()"
          v-if="showColumnsType == 'transfer'" />
        <el-dropdown
          trigger="click"
          :hide-on-click="false"
          style="padding-left: 12px"
          placement="bottom-end"
          :max-height="200"
          v-if="showColumnsType == 'checkbox'">
          <el-button
            circle
            icon="Menu" />
          <template #dropdown>
            <el-dropdown-menu>
              <!-- 全选/反选 按钮 -->
              <el-dropdown-item>
                <el-checkbox
                  :indeterminate="isIndeterminate"
                  v-model="isChecked"
                  @change="toggleCheckAll">
                  列展示
                </el-checkbox>
              </el-dropdown-item>
              <div class="check-line"></div>
              <template
                v-for="(item, key) in columns"
                :key="item.key">
                <el-dropdown-item :divided="key === 0">
                  <el-checkbox
                    v-model="item.visible"
                    @change="checkboxChange($event, key)"
                    :label="item.label" />
                </el-dropdown-item>
              </template>
            </el-dropdown-menu>
          </template>
        </el-dropdown>
      </el-tooltip>
    </el-row>
    <el-dialog
      :title="title"
      v-model="open"
      append-to-body>
      <el-transfer
        :titles="['显示', '隐藏']"
        v-model="value"
        :data="transferData"
        @change="dataChange"></el-transfer>
    </el-dialog>
  </div>
</template>

<script setup lang="ts">
  import cache from '@/plugins/cache'
  import type { TableShowColumns } from '@/types/api/common'

  const props = defineProps({
    /* 是否显示检索条件 */
    showSearch: {
      type: Boolean,
      default: true
    },
    /* 显隐列信息（数组格式、对象格式） */
    columns: {
      type: [Array, Object],
      default: () => ({})
    },
    /* 是否显示检索图标 */
    search: {
      type: Boolean,
      default: true
    },
    /* 显隐列类型（transfer穿梭框、checkbox复选框） */
    showColumnsType: {
      type: String,
      default: "checkbox"
    },
    /* 右外边距 */
    gutter: {
      type: Number,
      default: 10
    },
    /* 列显隐状态记忆的 localStorage key（传入则启用记忆，不传则不记忆） */
    storageKey: {
      type: String,
      default: ""
    }
  })

  const emits = defineEmits(['update:showSearch', 'queryTable'])

  // 显隐数据
  const value = ref<number[]>([])
  // 弹出层标题
  const title = ref("显示/隐藏")
  // 是否显示弹出层
  const open = ref(false)

  const style = computed(() => {
    const ret: Record<string, string> = {}
    if (props.gutter) {
      ret.marginRight = `${props.gutter / 2}px`
    }
    return ret
  })

  // 是否全选/半选 状态
  const isChecked = computed({
    get: () => Array.isArray(props.columns) ? props.columns.every((col: TableShowColumns) => col.visible) : Object.values(props.columns).every((col) => (col as TableShowColumns).visible),
    set: () => {}
  })
  const isIndeterminate = computed(() => Array.isArray(props.columns) ? props.columns.some((col: TableShowColumns) => col.visible) && !isChecked.value : Object.values(props.columns).some((col) => (col as TableShowColumns).visible) && !isChecked.value)
  const transferData = computed(() => Array.isArray(props.columns) ? props.columns.map((item: any, index: number) => ({ key: index, label: item.label })) : Object.keys(props.columns).map((key, index) => ({ key: index, label: (props.columns as any)[key].label })))

  // 搜索
  const { proxy } = getCurrentInstance()!
  function toggleSearch(): void {
    let el: HTMLElement | null = proxy!.$el
    let formEl: HTMLElement | null = null
    while ((el = el!.parentElement) && el !== document.body) {
      if ((formEl = el.querySelector('.el-form'))) break
    }
    if (!formEl) return emits('update:showSearch', !props.showSearch)
    // 优先动画整个 search-wrapper（el-card），避免表单先消失、卡片后消失的两段式抖动
    const targetEl = formEl.closest('.search-wrapper') as HTMLElement | null
    animateSearch(targetEl || formEl, props.showSearch)
  }
  function animateSearch(el: HTMLElement, isHide: boolean): void {
    const DURATION = 260
    const TRANSITION = 'max-height 0.25s ease, opacity 0.2s ease'
    const clear = () => Object.assign(el.style, { transition: '', maxHeight: '', opacity: '', overflow: '' })
    Object.assign(el.style, { overflow: 'hidden', transition: '' })
    if (isHide) {
      Object.assign(el.style, { maxHeight: el.scrollHeight + 'px', opacity: '1', transition: TRANSITION })
      requestAnimationFrame(() => Object.assign(el.style, { maxHeight: '0', opacity: '0' }))
      setTimeout(() => { emits('update:showSearch', false); clear() }, DURATION)
    } else {
      emits('update:showSearch', true)
      nextTick(() => {
        Object.assign(el.style, { maxHeight: '0', opacity: '0' })
        requestAnimationFrame(() => requestAnimationFrame(() => {
          Object.assign(el.style, { transition: TRANSITION, maxHeight: el.scrollHeight + 'px', opacity: '1' })
        }))
        setTimeout(clear, DURATION)
      })
    }
  }

  // 刷新
  function refresh(): void {
    emits("queryTable")
  }

  // 右侧列表元素变化
  function dataChange(data: any[], _direction?: string, _movedKeys?: any[]): void {
    if (Array.isArray(props.columns)) {
      for (let item in props.columns) {
        const key = props.columns[item].key
        props.columns[item].visible = !data.includes(parseInt(key))
      }
    } else {
      Object.keys(props.columns).forEach((key, index) => {
        props.columns[key].visible = !data.includes(index)
      })
    }
    saveStorage()
  }

  // 打开显隐列dialog
  function showColumn(): void {
    open.value = true
  }

  // 如果传入了 storageKey，从 localStorage 恢复列显隐状态
  if (props.storageKey) {
    try {
      const saved = cache.local.getJSON(props.storageKey)
      if (saved && typeof saved === 'object') {
        if (Array.isArray(props.columns)) {
          props.columns.forEach((col: TableShowColumns, index: number) => {
            if (saved[index] !== undefined) col.visible = saved[index]
          })
        } else {
          Object.keys(props.columns).forEach(key => {
            if (saved[key] !== undefined) props.columns[key].visible = saved[key]
          })
        }
      }
    } catch (e) {}
  }
  if (props.showColumnsType == "transfer") {
    // transfer穿梭显隐列初始默认隐藏列
    if (Array.isArray(props.columns)) {
      for (let item in props.columns) {
        if (props.columns[item].visible === false) {
          value.value.push(parseInt(item))
        }
      }
    } else {
      Object.keys(props.columns).forEach((key, index) => {
        if (props.columns[key].visible === false) {
          value.value.push(index)
        }
      })
    }
  }

  // 单勾选
  function checkboxChange(event: any, key: string | number): void {
    if (Array.isArray(props.columns)) {
      const col = props.columns.filter((item: any) => item.key == key)[0]
      if (col) {
        col.visible = event
      }
    } else {
      ;(props.columns as any)[key].visible = event
    }
    saveStorage()
  }

  // 切换全选/反选
  function toggleCheckAll(): void {
    const newValue = !isChecked.value
    if (Array.isArray(props.columns)) {
      props.columns.forEach((col: TableShowColumns) => (col.visible = newValue))
    } else {
      Object.values(props.columns).forEach((col) => ((col as TableShowColumns).visible = newValue))
    }
    saveStorage()
  }

  // 将当前列显隐状态持久化到 localStorage
  function saveStorage(): void {
    if (!props.storageKey) return
    try {
      let state: { [key: string]: boolean } = {}
      if (Array.isArray(props.columns)) {
        props.columns.forEach((col: TableShowColumns, index: number) => { state[index] = col.visible })
      } else {
        Object.keys(props.columns).forEach(key => { state[key] = props.columns[key].visible })
      }
      cache.local.setJSON(props.storageKey, state)
    } catch (e) {}
  }
</script>

<style lang="scss" scoped>
  :deep(.el-transfer__button) {
    border-radius: 50%;
    display: block;
    margin-left: 0px;
  }
  :deep(.el-transfer__button:first-child) {
    margin-bottom: 10px;
  }
  :deep(.el-dropdown-menu__item) {
    line-height: 30px;
    padding: 0 17px;
  }
</style>

```

### `TableToolbar`

`index.vue`

```vue
<template>
  <el-row
    :gutter="10"
    class="mb8">
    <!-- 前置自定义按钮 -->
    <slot name="prefix"></slot>

    <!-- 配置驱动的按钮组 -->
    <!-- v-hasPermi="btn.perm ? [btn.perm] : []" -->
    <template
      v-for="btn in visibleButtons"
      :key="btn.key">
      <el-col
        :span="1.5"
        style="margin:0px 0 8px 0;">
        <el-button
          v-hasPermi="btn.perm ? [btn.perm] : []"
          :type="resolveType(btn)"
          :plain="btn.plain !== false"
          :icon="resolveIcon(btn)"
          :size="btn.size || 'default'"
          :disabled="btn.disabled"
          @click="onClick(btn)">
          {{ resolveLabel(btn) }}
        </el-button>
      </el-col>
    </template>

    <!-- 后置自定义按钮 -->
    <slot></slot>

    <!-- 右侧工具栏（搜索开关/刷新/显隐列） -->
    <right-toolbar
      v-if="props.showRightToolbar"
      v-model:showSearch="innerShowSearch"
      :columns="props.columns"
      :search="props.showColumnSetting"
      :gutter="props.gutter"
      :storageKey="props.storageKey"
      @queryTable="emit('queryTable')"></right-toolbar>

    <!-- right-toolbar 之后的额外内容 -->
    <slot name="extra"></slot>
  </el-row>
</template>

<script setup lang="ts">
  import { computed } from "vue";
  import type {
    TableToolbarProps,
    TableToolbarEmits,
    ToolbarButton,
  } from "./types";
  import { DEFAULT_BUTTON_PRESET } from "./types";

  const props = withDefaults(defineProps<TableToolbarProps>(), {
    buttons: () => [],
    showRightToolbar: true,
    showSearch: true,
    gutter: 10,
    showColumnSetting: true,
  });

  const emit = defineEmits<TableToolbarEmits>();

  // 过滤隐藏按钮
  const visibleButtons = computed(() =>
    props.buttons.filter((b) => !b.hidden),
  );

  // showSearch 双向绑定
  const innerShowSearch = computed({
    get: () => props.showSearch,
    set: (val) => emit("update:showSearch", val),
  });

  // 解析按钮文案/类型/图标：优先用配置，否则回退到内置默认
  const resolveLabel = (btn: ToolbarButton) =>
    btn.label ?? DEFAULT_BUTTON_PRESET[btn.key]?.label ?? btn.key;

  const resolveType = (btn: ToolbarButton) =>
    btn.type ?? DEFAULT_BUTTON_PRESET[btn.key]?.type ?? "primary";

  const resolveIcon = (btn: ToolbarButton) =>
    btn.icon ?? DEFAULT_BUTTON_PRESET[btn.key]?.icon ?? "";

  const onClick = (btn: ToolbarButton) => {
    emit("action", btn.key, btn);
  };
</script>

```

`types.ts`

```ts
import type { Component } from "vue";

/** 内置按钮语义，覆盖项目里最常见的工具栏操作 */
export type ToolbarActionKey =
    | "add"
    | "edit"
    | "del"
    | "batch"
    | "import"
    | "export"
    | "download"
    | string;

/** 单个工具栏按钮配置 */
export interface ToolbarButton {
    /** 动作标识，点击后随 action 事件回传（如 'add' / 'export'），也用于匹配内置默认样式 */
    key: ToolbarActionKey;
    /** 按钮文案；不传则取内置默认（如 add -> "新增"） */
    label?: string;
    /** el-button 的 type；不传则取内置默认 */
    type?: "primary" | "success" | "warning" | "danger" | "info" | "";
    /** 按钮图标名（Element Plus 图标组件名，如 'Plus'）或图标组件；不传则取内置默认 */
    icon?: string | Component;
    /** 是否 plain 风格，默认 true（对齐项目现状） */
    plain?: boolean;
    /** 按钮尺寸，默认 'default' */
    size?: "large" | "default" | "small";
    /** 是否禁用 */
    disabled?: boolean;
    /** 是否隐藏该按钮（用于按权限/状态动态控制） */
    hidden?: boolean;
    /** 权限标识，透传给 v-hasPermi 指令（旧范式常用），为空则不加限制 */
    perm?: string;
}

export interface TableToolbarProps {
    /** 按钮配置数组，按顺序渲染 */
    buttons?: ToolbarButton[];
    /** 是否显示右侧 right-toolbar（搜索开关/刷新/显隐列），默认 true */
    showRightToolbar?: boolean;
    /** 搜索区显隐状态，配合 v-model:showSearch */
    showSearch?: boolean;
    /** 透传给 right-toolbar 的列显隐配置 */
    columns?: any[] | Record<string, any>;
    /** right-toolbar 的右外边距 gutter，默认 10 */
    gutter?: number;
    /** right-toolbar 列显隐状态本地存储 key */
    storageKey?: string;
    /** 是否显示 right-toolbar 内的显隐列/搜索开关图标（透传 search），默认 true */
    showColumnSetting?: boolean;
}

export interface TableToolbarEmits {
    /** 点击任一按钮，回传该按钮的 key（如 'add' / 'export'） */
    (e: "action", key: ToolbarActionKey, button: ToolbarButton): void;
    /** 搜索区显隐切换，配合 v-model:showSearch */
    (e: "update:showSearch", value: boolean): void;
    /** right-toolbar 刷新按钮点击，等价于原 @queryTable */
    (e: "queryTable"): void;
}

/** 内置按钮的默认样式映射（label / type / icon） */
export const DEFAULT_BUTTON_PRESET: Record<
    string,
    { label: string; type: ToolbarButton["type"]; icon: string }
> = {
    add: { label: "新增", type: "primary", icon: "Plus" },
    edit: { label: "修改", type: "success", icon: "Edit" },
    del: { label: "删除", type: "danger", icon: "Delete" },
    batch: { label: "批量删除", type: "danger", icon: "Delete" },
    import: { label: "导入", type: "info", icon: "Upload" },
    export: { label: "导出", type: "success", icon: "Upload" },
    download: { label: "下载模板", type: "warning", icon: "Tickets" },
};

```



### `CompTable`

`index.vue`

```vue
<template>
  <div class="comp-table">
    <!-- 顶部工具栏（列筛选 + 自定义按钮） -->
    <TableToolbar
      v-if="showColumnFilter"
      :buttons="buttons"
      :columns="columnVisibility"
      :storage-key="storageKey"
      :show-column-setting="showColumnFilter"
      v-model:show-search="innerShowSearch"
      @query-table="emit('queryTable')">
      <slot name="toolbar" />
    </TableToolbar>

    <!-- 表格 -->
    <el-table
      ref="ELTable"
      v-loading="list.loading"
      :data="list.items"
      element-loading-text="正在加载..."
      style="width: 100%"
      :max-height="tableHeight"
      :show-summary="showSummary"
      :summary-method="summaryMethod"
      border>
      <!-- 选择列 -->
      <el-table-column
        v-if="selectable"
        label="选择"
        :width="selectionWidth"
        align="center"
        fixed>
        <template #header>
          <el-checkbox v-model="isCheckAll" />
        </template>

        <template #default="{ row }">
          <el-checkbox
            v-model="row.checked"
            @change="handleChecked(row)" />
        </template>
      </el-table-column>

      <!-- 动态数据列 -->
      <el-table-column
        v-for="(col, idx) in visibleColumns"
        :key="col.key ?? col.prop ?? idx"
        :prop="col.prop"
        :label="col.label"
        :width="col.width"
        :min-width="col.minWidth ?? minWidth"
        :fixed="col.fixed"
        :align="col.align"
        :show-overflow-tooltip="col.showOverflowTooltip">
        <template #default="scope">
          <slot
            v-if="col.slot"
            :name="`col-${col.prop}`"
            :row="scope.row"
            :scope="scope" />
          <template v-else>{{ formatCell(scope.row, col) }}</template>
        </template>
      </el-table-column>
    </el-table>
  </div>
</template>

<script lang="ts" setup>
  import type { TableColumn } from './types'
  import type { PropType } from 'vue'
  import type { ToolbarButton } from '@/components/TableToolbar/types'

  import { defineListEmits, defineListProps, useListRef } from '@/hooks/useList'
  import {
    defineCheckAllEmits,
    defineCheckAllProps,
    useCheckAll,
  } from '@/hooks/useCheckAll'
  import { useFormatTable } from '@/hooks/useFormatTable'
  import { useTableHeight } from '@/hooks/useTable'
  import { useTempRefs } from '@/hooks/useTempRefs'
  import TableToolbar from '@/components/TableToolbar/index.vue'

  const props = defineProps({
    ...defineListProps(),
    ...defineCheckAllProps(),
    /** 列配置 */
    columns: {
      type: Array as PropType<TableColumn[]>,
      required: true,
    },
    /** 工具栏按钮配置 */
    buttons: {
      type: Array as PropType<ToolbarButton[]>,
      default: () => [],
    },
    /** 列显隐 localStorage key，不传则不持久化 */
    storageKey: {
      type: String,
      default: '',
    },
    /** 是否显示顶部列筛选下拉，默认 true */
    showColumnFilter: {
      type: Boolean,
      default: true,
    },
    /** 是否显示搜索区（由 TableToolbar 的搜索开关控制），默认 true */
    showSearch: {
      type: Boolean,
      default: true,
    },
    /** 是否启用选择列，默认 true */
    selectable: {
      type: Boolean,
      default: true,
    },
    /** 选择列宽度，默认 50 */
    selectionWidth: {
      type: [Number, String],
      default: 50,
    },
    /** 列最小宽度，默认 100 */
    minWidth: {
      type: [Number, String],
      default: 100,
    },
    /** 是否显示合计行，默认 false */
    showSummary: {
      type: Boolean,
      default: false,
    },
    /** 合计行计算方法 */
    summaryMethod: {
      type: Function as PropType<
        (param: { columns: any[]; data: any[] }) => (string | VNode)[]
      >,
      default: undefined,
    },
  })

  const emit = defineEmits([
    ...defineListEmits(),
    ...defineCheckAllEmits(),
    /** 刷新按钮点击 */
    'queryTable',
    /** 搜索区显隐切换 */
    'update:showSearch',
  ])

  /** showSearch 双向绑定：TableToolbar 切换 → 透传给父组件 */
  const innerShowSearch = computed({
    get: () => props.showSearch,
    set: (val: boolean) => emit('update:showSearch', val),
  })

  /** 表格自适应高度 */
  const { ELTable } = useTempRefs('ELTable')
  const { tableHeight, refresh: refreshTableHeight } = useTableHeight(ELTable)

  /** 搜索区显隐切换后表格 top 变化，需重新计算高度 */
  watch(
    () => props.showSearch,
    () => requestAnimationFrame(refreshTableHeight),
  )

  /** 列表 v-model:list 双向绑定 */
  const { list } = useListRef(props, { emit })

  /** 跨页选择 v-model:selected + 全选 + 单行勾选 */
  const { isCheckAll, handleChecked } = useCheckAll(
    props.keys,
    list,
    props,
    { emit },
  )

  /** 解析列的唯一 key */
  const resolveColKey = (col: TableColumn, idx: number): string =>
    col.key ?? col.prop ?? String(idx)

  /** 列显隐状态（RightToolbar 通过 v-model 直接修改 visible） */
  const columnVisibility = reactive(
    props.columns.map((col, idx) => ({
      key: resolveColKey(col, idx),
      label: col.label,
      visible: col.visible !== false,
    })),
  )

  /** 可见列（按 columns 原始顺序过滤） */
  const visibleColumns = computed(() =>
    props.columns.filter((col, idx) => {
      const key = resolveColKey(col, idx)
      const vis = columnVisibility.find(v => v.key === key)
      return vis ? vis.visible : true
    }),
  )

  /** 单元格格式化 */
  const formatCell = (row: any, col: TableColumn) => {
    const value = row?.[col.prop]
    const fmtType = col.format?.type
    if (!fmtType || fmtType === 'text') {
      return useFormatTable(value)
    }
    return useFormatTable(value, fmtType, col.format?.formatStr)
  }
</script>

<style lang="scss" scoped>
  :deep(.el-table) {
    transition: max-height 0.25s ease;
  }
</style>

```

`types.ts`

```ts
/** 列格式化配置（驱动 useFormatTable） */
export interface TableColumnFormat {
  /**
   * 格式化类型
   */
  type?: 'boolean' | 'date' | 'text'
  /** 格式化参数字符串 */
  formatStr?: string
}

/** 单列配置 */
export interface TableColumn {
  /** 唯一标识（prop 重复时必须传），兼作 localStorage 显隐 key */
  key?: string
  /** 数据字段名 */
  prop: string
  /** 列标题 */
  label: string
  /** 固定宽度 */
  width?: string | number
  /** 最小宽度，不传时自动调用 calcColumnMinWidth(label) */
  minWidth?: string | number
  /** 固定列 */
  fixed?: boolean | 'left' | 'right'
  /** 对齐方式 */
  align?: 'left' | 'center' | 'right'
  /** 内容溢出 tooltip */
  showOverflowTooltip?: boolean
  /** true → 使用具名插槽 col-{prop} 自定义渲染；与 format 互斥 */
  slot?: boolean
  /** useFormatTable 格式化配置；与 slot 互斥 */
  format?: TableColumnFormat
  /** 默认是否显示，默认 true */
  visible?: boolean
}

```

使用

```vue
<template>
  <div class="app-container">
    <TheSearch
      v-show="showSearch"
      v-model="list.query"
      :loading="list.loading"
      @search="getList" />

    <!-- 表格 start -->
    <CompTable
      v-model:list="list"
      v-model:selected="selected"
      v-model:show-search="showSearch"
      :keys="keys"
      :columns="columns"
      storage-key="acctPlatform:supplyChain:columns"
      @query-table="getList">
      <template #col-unitCost="{ row }">
        <CompPrice :price="row.unitCost" />
      </template>
      <template #col-acpAmount="{ row }">
        <CompPrice :price="row.acpAmount" />
      </template>
    </CompTable>
    <!-- 表格 end -->

    <pagination
      v-show="list.total > 0"
      :total="list.total"
      v-model:page="list.query.pageNum"
      v-model:limit="list.query.pageSize"
      @pagination="getList" />
  </div>
</template>

<script lang="ts" setup>
/** 列配置 */
  const columns: TableColumn[] = [
    { prop: 'generationStatus', label: '生成状态' },
    { prop: 'errorMsg', label: '错误信息', showOverflowTooltip: true },
    { prop: 'eventName', label: '事件' },
    { prop: 'companyName', label: '公司' },
    { prop: 'businessLineName', label: '业务线' },
    { prop: 'transactionTypeName', label: '事务处理类型' },
    { key: 'inOutOrderName1', prop: 'inOutOrderName', label: '原始单据编号' },
    { prop: 'originType', label: '原始单据类型' },
    { key: 'inOutOrderName2', prop: 'inOutOrderName', label: '出入库单' },
    { prop: 'date', label: '处理日期', format: { type: 'date', formatStr: 'YYYY年MM月DD日 HH时mm分mm秒' } },
    { prop: 'periodName', label: '期间', minWidth: 100 },
    { prop: 'locationName', label: '库位' },
    { prop: 'categName', label: '产品类别' },
    { prop: 'productName', label: '产品' },
    { prop: 'productSpec', label: '规格' },
    { prop: 'acpQuantity', label: '数量' },
    { prop: 'uomName', label: '单位' },
    { prop: 'unitCost', label: '单价', slot: true },
    { prop: 'acpAmount', label: '金额', slot: true },
    { prop: 'departmentName', label: '部门' },
    { prop: 'departmentAnalyticAccountName', label: '科室类型' },
    { prop: 'vendorName', label: '供应商' },
    { prop: 'projectName', label: '项目' },
    { key: 'transactionTypeName2', prop: 'transactionTypeName', label: '出入库类型' },
    { key: 'businessLineName2', prop: 'businessLineName', label: '业务类型' },
    { prop: 'createDate', label: '创建日期', format: { type: 'date', formatStr: 'YYYY年MM月DD日 HH时mm分mm秒' } },
    { prop: 'accountBookName', label: '账簿' },
  ]
</script>
```

